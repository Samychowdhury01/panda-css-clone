import Navbar from "../Navbar";

const MainLayout = () => {
    return (
        <div className="md:max-w-screen-xl md:mx-auto">
            <Navbar/>
        </div>
    );
};

export default MainLayout;